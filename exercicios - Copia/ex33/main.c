#include <stdio.h>
#include <stdlib.h>

int main()
{
    int l, a;
    printf("Informe o lado do quadrado: ");
    scanf("%d", &l);
    a=l*l;
    printf("A area do quadrado foi: %d\n", a);
    system("pause");
    return 0;
}
